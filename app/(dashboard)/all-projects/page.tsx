import AllProjectContent from "./components/AllProjectContent"
export const metadata = {
  title: "advators-all-projects",
};

const page = () => {
  return (
    <AllProjectContent />
  )
}
export default page